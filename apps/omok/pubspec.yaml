name: omok
description: 오목 게임 애플리케이션

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  cupertino_icons: ^1.0.8
  dio:
  flutter:
    sdk: flutter
  flutter_riverpod:
  freezed_annotation:
  get_it:
  injectable:
  json_annotation:
  riverpod_annotation:
  web_socket_channel:

dev_dependencies:
  build_runner:
  flutter_lints:
  flutter_test:
    sdk: flutter
  freezed:
  hive_generator:
  injectable_generator:
  json_serializable:
  mocktail:
  retrofit_generator:
  riverpod_generator:

flutter:
  uses-material-design: true